{
  "name": "video-flix",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server --watch listaVideoClipes.json --port $PORT --host 0.0.0.0"
  },
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
